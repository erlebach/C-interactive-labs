"""Subject: basic raw pointer (`int* p = &val;`).

Page package for the basic_ptr lesson: `basic_ptr.page.yaml` (the page spec),
`topics.py` (the topic set, re-exported from the Pointers & References lab), and
`test_basic_ptr.py` (build integration). The page is rendered by the shared
engine in `cpp_ptr_lab/yaml_engine/render_page.py`.
"""
