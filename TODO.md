# TODO list and ideas

- Add documentation to all modules (should be in memory)
- docs should be added to instructions in spec or proposal. 
- lint instructions should be defined in specs or proposal 
   (as a way of allowing for lower caliber model to do the coding)
- the interface should add value relative to `cppreference`. That would 
  be interactivity, which was present in the `dearpygui` interface, but 
  not really in the current html version. The demos should also be 
  interesting for the students. 

# C++ interactive modules

- initializers
- pointers and references (basic)
- pointers and references (smart)

- Stack frame (recursion, non-recursion, heap, stack))

- class structure (ctor, cctor, assign, move)
  (return types, argument types)

- function arguments (value, pointer, reference)

- templates (single, multiple, specialized)

- STL (explore via discussion)

- 
