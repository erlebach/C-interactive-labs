# HANDOFF — 2026-07-06 07h14mEST

**Focus for the next session:** Finish the `skill/demonstration-builder` branch — the
`demonstration-builder` skill + `template_subject` exemplar are complete, committed, and green;
decide integration (merge locally or open a PR) via `superpowers:finishing-a-development-branch`.
Optionally revisit the *triggering-description* improvement by hand (the automated optimizer is
unreliable here — see Decisions).

## Read first / references
- **`docs/superpowers/specs/2026-07-05-demonstration-builder-skill-design.md`** — the design (engine
  = in-repo reference; interactive workflow; the renderer-availability diagram triage + decisions 3/3b).
- **`docs/superpowers/plans/2026-07-05-demonstration-builder-skill.md`** — the executed 7-task TDD plan.
- **`.claude/skills/demonstration-builder/`** — the shipped skill (`SKILL.md`, `reference/`,
  `templates/`, `scripts/scaffold_subject.sh`).
- **`cpp_labs/template_subject/`** — the worked exemplar (2 examples + 1 gotcha, `diagram:false`).
- **`JOURNAL.md`** head (2026-07-06 00:18 + the new entry) — session narrative.
- Prior handoff `handoffs/HANDOFF_2026-07-05_20h43mEST.md` — **superseded** (std-variant-axis, merged PR #6).

## What changed this session
- **Branch cleanup:** deleted local+remote `feat/std-variant-axis` (PR #6 was already **merged**);
  `main` fast-forwarded to `11f87f8`. New branch **`skill/demonstration-builder`**.
- **Built the skill via brainstorm → spec → plan → subagent-driven TDD** (Sonnet 5 implementers,
  Opus controller). 16 commits `f3e950b..a190853`. Skill = lean `SKILL.md` + `reference/{PATTERN,
  DIAGRAMS,CHECKLIST}.md` + `templates/` + guard test `cpp_labs/tests/test_demonstration_skill.py`.
  **Zero engine code change** (references the in-repo `cpp_labs/` engine).
- **Exemplar `cpp_labs/template_subject/`** (`ecc60b3`,`a4d848d`,`956f0c6`) — TDD against exact baked
  g++ stdout.
- **Two spec-review catches, both fixed:** the gotcha test asserted a vacuous `out--err` CSS class →
  now `"Compile failed"` badge (`207b7d9`); `DIAGRAMS.md` wrongly listed `frames_anatomy` as a PTRDATA
  type and mis-described the empty-cell mechanism (`b69081e`).
- **skill-creator best-practice pass (post-Task-7):**
  - `42b8f53` — **fixed two real correctness bugs in `SKILL.md`**: a non-runnable build command
    (`python -m cpp_labs.build_html` → `./build_labs.sh <subject>`) and a wrong test path
    (`cpp_labs/tests/` → subject-local `cpp_labs/<subject>/tests/`).
  - `a190853` — **bundled `scripts/scaffold_subject.sh`** (creates a consistent, immediately
    buildable+green minimal page; verified end-to-end on a throwaway subject) and wired it into Step 4.
- **Verification (Task 7, commit `24a4967`):** `./build_labs.sh` → **built 10, failed 0**;
  `template_subject` + guard tests **9 passed**; full engine suite **391 passed**; interface catalog
  **4 passed** (no drift). Scaffold script separately verified: `built 1, failed 0`, its 4 tests pass,
  overwrite-guard refuses.

## Decisions locked
- **Do NOT apply the auto-"optimized" triggering description.** The skill-creator `run_loop.py`
  optimizer was run and **killed**: it measured **recall=0% across every description and iteration**
  (the skill never triggered under the `claude -p` eval harness), so accuracy was a degenerate 50%
  everywhere and no proposal could be shown to beat another. It also ran ~6.5 h for 4 iterations. The
  committed `SKILL.md` keeps its **original hand-written description**. If improving triggering, do it
  as a *reviewed manual edit* — the strongest idea from the run was adding an explicit **"Do NOT use
  for: editing the cpp_labs engine, fixing build scripts, unit-testing engine code, reviewing diffs,
  answering general C++ questions"** negative-scope clause (targets the near-miss queries).
- **Engine relationship = in-repo reference (v1);** self-contained bundling deferred.
- **Diagram triage keyed on renderer availability, not pointers;** case-2 (no renderer) = redraw an
  author image as a hand-authored SVG else `diagram:false`; the case-2 engine block is deferred.
- **Scaffold produces a *buildable* baseline** (example id `<subject>_ex1`, prints `x = 42`) to grow
  from — this also serves as a template-consistency check.

## Next steps
1. **Finish the branch** — `superpowers:finishing-a-development-branch` (merge locally or open a PR).
   `main` is untouched at `11f87f8`; all work is on `skill/demonstration-builder`.
2. **(Optional) Manual description refinement** — add the negative-scope clause above to `SKILL.md`
   frontmatter; keep the current pushy positive triggers. Re-run the guard test (frontmatter still
   valid). Do NOT re-run `run_loop.py` expecting signal until trigger-detection under `claude -p` is
   understood.
3. **(Optional) Grow a real subject** via the new skill/scaffold (e.g. move semantics, RAII, vector) —
   the eval loop's *content* path was never validated; a real authoring run is the honest test.
4. **(Deferred, documented seams)** case-2 author-image SVG engine block; interactive-diagram template
   components; engine bundling; a working skill-creator eval loop.

## Constraints still in force
- Run from repo root. Rebuild `./build_labs.sh [filter]`; serve `python3 -m http.server -d dist_labs 8000`.
  Full suite compiles C++ (~mins). `dist_labs/` is gitignored — never commit baked HTML.
- **Minimize-JS / WCAG-AA**; every `<svg>` via `_wrap_svg`; interface catalog is generated (regen only
  on a components *signature* change).
- Course authoring conventions: `class` not `struct`; comments on their own line above code; break long
  `<<` chains aligned; tests assert **byte-identical** baked stdout.
- **Skill-list noise:** many stale `demonstration-builder-skill-<hash>` entries are leftover harness
  registrations from the killed optimizer's temp `claude -p` copies — **not in the repo** (repo
  `.claude/skills/` has only `demonstration-builder`); they clear on context reset. `/tmp/db_*.json`,
  `/tmp/eval_review_*.html`, `/tmp/db_descopt.log` are scratch and can be discarded.
- **Do NOT commit** repo-root scratch: `session-*.md`, `prototype/`, `a.cpp`, `a.md`, `run.x`,
  `harness.md`, `TODO_NEXT.md`, `cpp_ptr_lab/pointers_refs/JOURNAL.md`, `"I created this interface…md"`,
  the `TODO.md` working-tree mod.

## Suggested skills
- **superpowers:finishing-a-development-branch** — integrate/clean up the branch (next action).
- **demonstration-builder** — the skill just built; use it (or `scripts/scaffold_subject.sh`) to author
  the next real cpp_labs subject.
- **superpowers:subagent-driven-development** — if growing a real subject from a plan (worked well: Sonnet
  implementers + two-stage spec/quality review caught the two real bugs this session).
